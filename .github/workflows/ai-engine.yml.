‏name: AI Cyber Security Scan

‏on:
‏  push:
‏    branches: [ main ]

‏jobs:
‏  analyze:
‏    runs-on: ubuntu-latest
‏    steps:
‏      - name: Checkout Code
‏        uses: actions/checkout@v3

‏      - name: Run AI Security Analysis
‏        env:
‏          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
‏        run: |
         
‏          python scripts/analyze_code.py
